# Environment variables for metabrowse editor build
# Copy to .env.local (gitignored) for local development

# The GitHub host to use (defaults to 'github.com' if not set during build)
VITE_DEFAULT_HOST=your-github-host.example.com
